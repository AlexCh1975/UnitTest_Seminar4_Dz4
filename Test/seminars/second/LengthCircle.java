package seminars.second;

public class LengthCircle {
    public double computeLengthCircle(int i) {
        double length;
        length = Math.round(2 * Math.PI * i);
        return length;
    }
}
